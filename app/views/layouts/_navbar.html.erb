<nav class="navbar navbar-dark bg-inverse">
  <a class="navbar-brand" href="#"></a>
  <ul class="nav navbar-nav">
    <li class="nav-item nav-link">
    <% if current_user %>
       <img src="<%= profile_picture %>"><%= current_user.name %>
      <%= link_to "Logout", logout_path, method: :delete %>
    <% else %>
      <%=  link_to "Sign in with FaceBook", facebook_login_path %>
    <% end %>
    </li>
    <li class="nav-item active">
      <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#"> Current City : <%= geo_location[:city]%>&nbsp;|&nbsp;<%= geo_location[:temp] %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">About</a>
    </li>
    <form class="form-inline pull-xs-right" action='/weathers/new' >
      <input name="zip" class="form-control" type="text" placeholder= "Zipcode", method: 'get'>
      <button class="btn btn-success-outline" type="submit">Search</button>
    </form>
  </ul>
</nav>
